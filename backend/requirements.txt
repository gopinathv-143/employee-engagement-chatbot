# --- Core data handling ---
pandas>=2.2
openpyxl>=3.1
numpy>=1.26

# --- Config ---
python-dotenv>=1.0
pydantic>=2.6

# --- Groq SDK (used directly for the agent's tool-calling loop, SQL
# generation, and sentiment classification - OpenAI-compatible chat API) ---
groq

# --- LlamaIndex (retrieval over employee free-text comments). Embeddings run
# locally via HuggingFace/sentence-transformers - no embedding API key
# needed; Groq is the only API key this project uses. ---
llama-index-core
llama-index-embeddings-huggingface

# --- API layer ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Testing (structured-data tools only; no API key required) ---
pytest>=8.0

# --- Retry/backoff for Groq rate limits ---
tenacity>=9.0
